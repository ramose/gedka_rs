import 'package:gedka_rs/tokens/colors.dart';
import 'package:flutter/material.dart';

class ColorFoundation {
  ColorFoundation._();

  static const Color activeButtonColor = AppColor.primary;
  static const Color disabledButtonColor = AppColor.lightPrimary;

  static const Color activeIconColor = AppColor.lightPrimary;
  static const Color disabledIconColor = AppColor.lightPrimary;

  static const Color activeFontColor = AppColor.primary;
  static const Color disabledFontColor = AppColor.lightPrimary;
  
  static const Color activeBgColor = AppColor.primary;
  static const Color disabledBgColor = AppColor.lightPrimary;

}
